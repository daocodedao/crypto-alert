version: '3'
services:
  freshrss:
    image: lscr.io/linuxserver/freshrss:latest
    container_name: freshrss
    ports:
      - "3080:80"
    volumes:
      - /data/work/freshrss/data:/config
    environment:
      - TZ=Asia/Shanghai  # 设置时区
      - PUID=1000
      - PGID=1000
    restart: always